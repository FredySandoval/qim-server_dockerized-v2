name: Build and Deploy

on:
  push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1
      
      - name: Login to Docker Hub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKER_HUB_USERNAME }}
          password: ${{ secrets.DOCKER_HUB_PASSWORD }}
          
      - name: Build and push the image
        run: DOCKER_BUILDKIT=1 docker build . --file Dockerfile --tag qimimage:latest
        
      - name: Push the Docker image
        run: docker push shockerovip/qimrepo:latest
       # docker push shockerovip/qim-server_dockerized-v2:latest
       # docker login --username ${{ secrets.DOCKER_HUB_USERNAME }} --password ${{ secrets.DOCKER_HUB_PASSWORD }}
       
      #repository: ${{ github.repository }}
      #tag_with_ref: true
      #tag_with_sha: true
      #tags: ${{ github.sha }}
    
    #- name: Build the Docker image
      #run: docker build . --file Dockerfile --tag my-image-name:$(date +%s)
    #  run: |
    #    docker build -t username/repo-client:latest ./client
    #   docker push username/repo-client:latest
